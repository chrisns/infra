apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - github.com/kubernetes-sigs/external-dns/kustomize?ref=v0.12.0

resources:
  - namespace.yaml

secretGenerator:
  - name: cloudflare
    env: .env
    type: Opaque

patches:
  - deployment.yaml

namespace: external-dns